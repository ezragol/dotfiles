
# #######################################################################################
# AUTOGENERATED HYPR CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

# autogenerated = 1 # remove this line to remove the warning

# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/Configuring-Hyprland/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf

################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=,highres,auto,1


###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = alacritty --class zsh
$fileManager = alacritty --class file_explorer -e joshuto
$menu = dmenu --show drun

#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# exec-once = nm-applet &
exec-once = hyprpaper
exec-once = hypridle


# Eww setup
exec-once = eww daemon
exec = eww open dock
exec = eww open desktop
exec = rm /home/ezra/.config/eww/volid.txt /home/ezra/.config/eww/mute.txt /home/ezra/.config/eww/volume.txt /home/ezra/.config/eww/micid.txt /home/ezra/.config/eww/micmute.txt
exec = touch /home/ezra/.config/eww/volid.txt /home/ezra/.config/eww/mute.txt /home/ezra/.config/eww/volume.txt /home/ezra/.config/eww/micid.txt /home/ezra/.config/eww/micmute.txt

# braindead but works please don't change
# eww is too scuffed for anything else 
# exec = export VOL=no
exec = /home/ezra/.config/eww/mute.sh -q && /home/ezra/.config/eww/mute.sh -q && /home/ezra/.config/eww/micmute.sh -q && /home/ezra/.config/eww/micmute.sh -q
exec = /home/ezra/.config/eww/volup.sh -q && /home/ezra/.config/eww/voldown.sh -q

exec-once = dunst &
# exec = dunstctl rule dnd enable
exec = dunstctl rule dnd disable


exec = eww update dnd=false
exec = eww update caps=$(hyprctl devices -j | jq -c '.keyboards | .[] | select(.main) .capsLock')
exec = /home/ezra/.config/eww/wtitle.sh
exec = /home/ezra/.config/eww/workspace.sh

# plugins
exec = hyprctl plugin load /home/ezra/.config/hypr/plugin/plugin.so

#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = XCURSOR_SIZE,15
env = HYPRCURSOR_SIZE,16


#####################
### LOOK AND FEEL ###
#####################


# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general { 
    gaps_in = 2
    gaps_out = 0

    border_size = 0

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = true 

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle

    snap {
        enabled = true
        monitor_gap = 30
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 0

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 0.85

    shadow {
        enabled = false
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 2
        passes = 1

        vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = true

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 8, default
    animation = workspaces, 1, 6, default
}

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc { 
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
    middle_click_paste = false
}

#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
        tap-to-click = false
        scroll_factor = 0.3
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
    workspace_swipe = false
}

####################
### KEYBINDINGSS ###
####################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

bind = $mainMod, Return, exec, $terminal
bind = $mainMod, Q, killactive,
bind = $mainMod, M, exit,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating,
bind = $mainMod, R, exec, $menu
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # dwindle

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = Alt, mouse:272, resizewindow

# Brightness
binde = , code:73, exec, /home/ezra/.config/hypr/brightness_down.sh
binde = , code:74, exec, /home/ezra/.config/hypr/brightness_up.sh

# Volume
bindr = , code:67, exec, /home/ezra/.config/eww/mute.sh
binde = , code:68, exec, /home/ezra/.config/eww/voldown.sh
binde = , code:69, exec, /home/ezra/.config/eww/volup.sh

binde = , code:70, exec, /home/ezra/.config/eww/micmute.sh

bindl = , Print, exec, hyprshot -m region output -o ~/Images/screenshots

bind = Alt, Return, exec, wofi --show drun
bind = $mainMod, space, exec, wofi --show drun

bind = $mainMod, Tab, workspace, previous

bindr = CAPS, Caps_Lock, exec, eww update caps=$(hyprctl devices -j | jq -c '.keyboards | .[] | select(.main) .capsLock')

bind = Alt, 1, exec, /home/ezra/.config/eww/select.sh google-chrome google-chrome-stable --ozone-platform-hint=auto --ozone-platform=wayland &
bind = Alt, 2, exec, /home/ezra/.config/eww/select.sh Spotify spotify &
bind = Alt, 3, exec, /home/ezra/.config/eww/select.sh Code code

bind = Alt, R, exec, /home/ezra/.config/eww/show.sh showRam
bind = Alt, T, exec, /home/ezra/.config/eww/show.sh showTemp

##############################
### WINDOWS AND WORKSPACES ###
##############################

windowrulev2 = tile,class:^(MATLAB),title:^(.*)$
windowrulev2 = opacity 1,class:,title:

windowrulev2 = float,class:^(.*)$,title:^(Save File)$
windowrulev2 = float,class:^(.*)$,title:^(Open File|Open Files)$
windowrulev2 = size 60% 70%,class:^(.*)$,title:^(Save File)$
windowrulev2 = size 60% 70%,class:^(.*)$,title:^(Open File|Open Files)$
windowrulev2 = suppressevent maximize, class:.* # You'll probably like this.
windowrulev2 = noblur,class:^()$,title:^()$

windowrulev2 = noshadow,floating:0
windowrulev2 = rounding 0,floating:0
windowrulev2 = bordercolor rgba(0,0,0,0),floating:0

windowrulev2 = noblur,class:^(wofi)$

layerrule = blur, gtk-layer-shell
layerrule = ignorealpha 0.2, gtk-layer-shell

animation = workspaces, 1, 8, default, slidefade 20%

layerrule = animation slide right, notifications